import type {Metadata} from "next";
import ToolsDirectoryClient from "@/components/ToolsDirectoryClient";import {createServerSupabase} from "@/lib/supabase-server";import {getSiteUrl} from "@/lib/site-url";import type {AITool} from "@/lib/tool-types";
export const revalidate=300;
export const metadata:Metadata={title:"Direktori AI Tools: Temukan Tool AI Terbaik | AIUpdateId",description:"Jelajahi direktori AI Tools untuk menulis, belajar, riset, coding, produktivitas, gambar, dan bisnis. Bandingkan fitur, harga, kelebihan, kekurangan, dan rating.",alternates:{canonical:"/tools"},openGraph:{title:"Direktori AI Tools | AIUpdateId",description:"Temukan dan bandingkan AI Tools berdasarkan kebutuhan Anda.",type:"website"}};
export default async function ToolsPage(){const supabase=createServerSupabase();let tools:AITool[]=[];if(supabase){const{data}=await supabase.from("ai_tools").select("*").eq("status","published").order("featured",{ascending:false}).order("rating",{ascending:false}).order("name",{ascending:true});tools=(data||[]) as AITool[]}const base=getSiteUrl();const schema={"@context":"https://schema.org","@type":"CollectionPage",name:"Direktori AI Tools AIUpdateId",url:`${base}/tools`,description:"Direktori tool Artificial Intelligence untuk pengguna Indonesia.",mainEntity:{"@type":"ItemList",itemListElement:tools.slice(0,50).map((t,i)=>({"@type":"ListItem",position:i+1,url:`${base}/tools/${t.slug}`,name:t.name}))}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><ToolsDirectoryClient tools={tools}/></>}
