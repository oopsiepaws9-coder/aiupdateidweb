# AIUpdateId Public V9.4.1 — Compare Publish Cache Fix

Fixes stale 404 behavior when a comparison is changed from `draft` to `published`.

Changes:
- `/compare/[slug]` is dynamic and revalidates on every request.
- `/compare` directory refresh interval reduced to 60 seconds.
- `sitemap.xml` dynamic CMS URLs refresh every 60 seconds.
- Existing V9.4.0 article consolidation/redirect is preserved.

Expected verification after deploy:
- published comparison detail => HTTP 200
- draft comparison detail => HTTP 404
