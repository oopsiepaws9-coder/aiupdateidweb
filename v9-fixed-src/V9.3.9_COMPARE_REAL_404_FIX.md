# AIUpdateId Public V9.3.9 — Compare Real 404 Fix

Perubahan:
- `/compare/[slug]` sekarang memanggil `notFound()` jika comparison tidak ada atau belum berstatus `published`.
- Menghilangkan soft-404 HTTP 200 untuk comparison draft/tidak ditemukan.
- Comparison published tetap dirender normal dan tetap menggunakan metadata/canonical/schema yang sudah ada.
- Tidak mengubah template artikel, sitemap, robots, atau server-first rendering V9.3.8.
