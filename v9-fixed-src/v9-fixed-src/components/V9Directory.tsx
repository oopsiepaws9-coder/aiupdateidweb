"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";import {Search,Star} from "lucide-react";
export default function V9Directory({table,title,base}:{table:string;title:string;base:string}){const[items,setItems]=useState<any[]>([]);const[q,setQ]=useState("");useEffect(()=>{supabase.from(table).select("*").eq("status","published").order("created_at",{ascending:false}).then(({data})=>setItems(data||[]))},[table]);const f=items.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase()));return <main className="page"><section className="container intro"><small>V9 DIRECTORY</small><h1>{title}</h1><label className="globalSearchBox"><Search size={20}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari..."/></label></section><section className="container compact"><div className="v9DirectoryGrid">{f.map(x=><Link href={`${base}/${x.slug}`} key={x.id}><div className="toolLogo">{(x.name||x.term||x.title).slice(0,2)}</div><small>{x.category||x.provider||"AIUpdateId"}</small><h2>{x.name||x.term||x.title}</h2><p>{x.short_description||x.short_definition||x.summary}</p>{x.rating!=null&&<b><Star size={15}/>{x.rating}/10</b>}</Link>)}</div>{!f.length&&<div className="empty">Belum ada data.</div>}</section></main>}
